@model IEnumerable<District3_APP_WEB.Models.Highlight>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostsIds)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoverFilePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostsIds)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoverFilePath)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HighlightId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HighlightId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HighlightId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
