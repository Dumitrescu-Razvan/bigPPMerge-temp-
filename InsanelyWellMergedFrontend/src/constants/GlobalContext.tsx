import React from 'react';

// eslint-disable-next-line react-refresh/only-export-components
export const globalData = {
  example: 'contextGlobalExempluCox'
};

export const GlobalContext = React.createContext(globalData);
