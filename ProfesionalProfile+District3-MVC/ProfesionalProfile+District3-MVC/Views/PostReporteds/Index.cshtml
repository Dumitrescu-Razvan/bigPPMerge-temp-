@model IEnumerable<ProfesionalProfile_District3_MVC.Models.PostReported>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Reason)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reporter_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Reason)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reporter_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Report_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Report_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Report_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
