@model IEnumerable<ProfesionalProfile_District3_MVC.Models.AssessmentTest>

@{
    ViewData["Title"] = "Index";
}

<h1>AssessmentTest</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.testName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skill)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.testName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Skill.skillId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.assessmentTestId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.assessmentTestId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.assessmentTestId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
