@model IEnumerable<ProfesionalProfile_District3_MVC.Models.AssessmentResult>

@{
    ViewData["Title"] = "Index";
}

<h1>Assessment Result</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.score)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.testDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssessmentTest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.score)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.testDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssessmentTest.assessmentTestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.assesmentResultId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.assesmentResultId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.assesmentResultId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
